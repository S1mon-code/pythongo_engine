.DS_Store
__pycache__/
*.pyc
*.zip
*.rar
./state/
.claude/worktrees/
.pytest_cache/

# 实盘 log — 保留 StraLog.txt 作为历史证据, 后续新 log 不再 commit
logs/*.txt
!logs/StraLog.txt

# PythonGO vendor SDK — 全部 commit 进 repo (含 .pyd Windows binary / .png / .qss),
# 作为 SDK 完整快照. 2026-04-20 实盘验证完成后 Simon 要求把这些全部归档.
